query searchFacets($input: SearchFacetsInput!) {
  searchFacets(input: $input) {
    categories {
      key
      value
      name
    }
    colors {
      key
      value
      name
      hex
    }
    sizes {
      key
      value
      name
    }
    prices {
      from
      to
    }
  }
}
