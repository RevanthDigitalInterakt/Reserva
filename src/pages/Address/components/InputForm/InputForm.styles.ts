import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  inputContainer: {
    borderWidth: 1,
    borderColor: '#656565',
    borderRadius: 5,
  },

  inputText: {
    marginHorizontal: 10,
  },
});

export default styles;
