import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  topBarDefault: {
    zIndex: 999,
    position: 'absolute',
  },
  transparentTopBar: {
    zIndex: 999,
  },
  whiteTopBar: {
    zIndex: 999,
    position: 'absolute',
  },
});

export default styles;
