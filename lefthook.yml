pre-commit:
commit-msg:
  commands:
    commitlint:
      run: npx commitlint --edit
      skip: merge
pre-push:
  parallel: true
  commands:
    lint:
      run: bash scripts/check.sh
      skip: merge
    #test: #TODO fix tests
    #  run: bash scripts/tests.sh
    #  skip: merge